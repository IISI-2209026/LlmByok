## 1. Pi Runner 實作

- [x] 1.1 [P] 實作 `BuildPiEnv` 函式於 `internal/runner/pi.go`：以 `os.Environ()` 為起點，覆寫 `PI_CODING_AGENT_DIR=<tempDir>`，其餘環境變數保持不變，回傳 `[]string`。父程序環境永不被修改。對應 design「PI_CODING_AGENT_DIR + 臨時 models.json 注入方式」與「使用 resolveProfileForLaunch 共用輔助函式」。驗證：`internal/runner/pi_test.go` 的 `TestBuildPiEnv_SetsPiCodingAgentDir` 確認子程序環境包含 `PI_CODING_AGENT_DIR=<tempDir>` 且父程序環境不含此變數。
- [x] 1.2 [P] 實作 `LaunchPi` 函式於 `internal/runner/pi.go`：建立臨時目錄（`os.MkdirTemp`），寫入 `models.json`（JSON 結構 `{"providers":{"openai":{"baseUrl":"<profile.APIBase>","apiKey":"<profile.APIKey>"}}}`），以 `BuildPiEnv` 組裝子程序環境，將 `--model <modelOverride 或 profile.DefaultModel>` 附加到 extraArgs 前端，以 `exec.Command(exePath, args...)` 啟動 pi 子程序，`defer os.RemoveAll(tempDir)` 清理臨時目錄。對應 design「PI_CODING_AGENT_DIR + 臨時 models.json 注入方式」、「--model 透過 CLI 旗標傳遞」與「臨時目錄清理」。涵蓋 spec「Launch Pi with BYOK profile」、Pi executable presence check 的子程序啟動、Parent process environment unchanged for pi 的環境隔離。驗證：`internal/runner/pi_test.go` 的 `TestLaunchPi_CreatesTempDirWithModelsJson` 確認臨時目錄含正確 `models.json` 且子程序命令列含 `--model <default_model>`，`TestLaunchPi_OverrideModel` 確認 `--model` 覆寫值正確，`TestLaunchPi_CleansUpTempDir` 確認子程序結束後臨時目錄不存在，`TestLaunchPi_UserConfigNotModified` 確認 `~/.pi/agent/models.json` 不被建立或修改。

## 2. Pi Launch 指令實作

- [x] 2.1 [P] 新增 `runLaunchPi` 函式於 `cmd/launch_pi.go`：呼叫 `resolveProfileForLaunch(cfgPath, profileName, piBinary, piInstallHint, stderr)` 共用輔助函式（步驟 1–6：解析設定檔路徑、載入設定檔、選擇 profile、provider 驗證、LookPath、解析金鑰），再以 `runner.LaunchPi(profile, model, resolved, extraArgs, os.Stdin, stdout, stderr)` 啟動子程序。`piBinary = "pi"`，`piInstallHint = "請先安裝 pi CLI。參見 https://pi.dev/docs/latest"`。涵蓋所有錯誤路徑：Pi missing config file error（設定檔不存在印提示並 exit 1）、Pi missing profile error（profile 找不到列出可用名稱並 exit 1）、Pi provider validation（非 openai 拒絕並 exit 1）、Pi executable presence check（pi 不在 PATH 印安裝提示並 exit 1）、金鑰找不到印 `byok config set-key` 提示並 exit 1。pi 以非零結束碼結束時靜默傳遞 exit 1。對應 design「使用 resolveProfileForLaunch 共用輔助函式」。涵蓋 spec「Pi argument passthrough via double dash」：`--` 後的參數原樣附加到 pi 子程序命令列。驗證：`cmd/launch_pi_test.go` 涵蓋預設 profile、`--model` 覆寫、`--profile` 選擇、設定檔不存在、profile 不存在、非 openai provider、pi 不在 PATH、金鑰找不到、`--` 透傳單一/多個參數等情境。

## 3. Dispatch 更新

- [x] 3.1 更新 `cmd/launch.go` 的 dispatch switch 與錯誤訊息：在 `switch target` 新增 `case "pi": return runLaunchPi(...)` 分派，更新錯誤訊息列出 `copilot、codex、codex-app、claude、pi` 為支援的目標工具，更新 `Short` 描述與 `Long` 說明文字納入 pi。涵蓋 spec「Target tool selection and dispatch」的 pi 分派與 unsupported target 錯誤訊息。驗證：`cmd/launch_dispatch_test.go` 新增 pi dispatch 測試 case 確認 `byok launch pi` 分派到 pi launch flow，`byok launch gemini` 錯誤訊息列出 pi。
- [x] 3.2 更新 `cmd/launch.go` 的 usage template 與 Example：在 Targets 區塊新增 `pi     以 BYOK profile 啟動 pi CLI`，在 Example 新增 `byok launch pi`、`byok launch pi -y` 等範例。涵蓋 spec「Target tool selection and dispatch」中 pi 為支援目標。驗證：`cmd/launch_dispatch_test.go` 確認 usage template 含 pi。
- [x] 3.3 更新 `cmd/launch.go` 的 `buildExtraArgs` 函式：新增 `pi` target 的 yolo 映射，當 `target == "pi"` 時 `yoloLiteral = "--approve"`。對應 design「--yolo 映射為 --approve」。涵蓋 spec「Pi YOLO mode flag」。驗證：`cmd/launch_pi_test.go` 的 `TestRunLaunchPi_YoloFlag` 確認 `--yolo` 時 pi 子程序命令列含 `--approve`，`TestRunLaunchPi_YoloShortForm` 確認 `-y` 同效，`TestRunLaunchPi_YoloWithPassthrough` 確認 `--approve` 在透傳參數前。

## 4. 文件更新

- [x] 4.1 [P] 更新 `README.md`：將簡介文字從「Copilot CLI、Codex CLI 或 Claude Code」改為涵蓋 pi；在「運作原理」新增「Pi BYOK」小節，說明 `PI_CODING_AGENT_DIR` 環境變數、臨時 `models.json` 覆寫 `providers.openai.baseUrl` 與 `providers.openai.apiKey`、`--model` CLI 旗標、`~/.pi/agent/models.json` 不被修改；在 Targets 表新增 `pi` 列；在 launch 範例新增 `byok launch pi` 等範例；在「官方文件」新增 https://pi.dev/docs/latest/providers 連結；在「支援的目標工具」與前置需求更新納入 pi；在 `--yolo` 說明新增 pi 映射 `--approve`。涵蓋 spec「Pi launch documentation in README」的三個情境（targets table、BYOK section、official documentation links）。驗證：手動審查 README.md 含 pi targets table 列、Pi BYOK 小節、官方文件連結。
- [x] 4.2 [P] 更新 `AGENTS.md`：在「套件職責」表格更新 `internal/runner` 行新增 pi 職責說明、`cmd` 行新增 pi launch 分派；在「開發規範」的 BYOK 注入段落新增 pi 的 `PI_CODING_AGENT_DIR` + 臨時 models.json 機制說明、不寫入使用者設定檔段落新增 `~/.pi/agent/models.json`。涵蓋 CLI 介面與 BYOK 注入機制的維護規則。驗證：手動審查 AGENTS.md 的 `internal/runner` 與 `cmd` 職責列含 pi、BYOK 注入段落含 pi 說明。
