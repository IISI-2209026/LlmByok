## 1. Release tag commitish fix

- [x] 1.1 In the `release` job of `.github/workflows/release.yml`, add `target_commitish: ${{ github.sha }}` to the `softprops/action-gh-release` step so the GitHub Release tag is created on the commit that triggered the workflow (develop commit for develop pushes, main commit for main pushes) rather than the repository default branch. This satisfies the "Branch-specific release tag and prerelease flag" requirement and the "Decision: Set `target_commitish` to `github.sha` on the release action". Verification: review the workflow YAML confirms the input is present on the release action step.
- [x] 1.2 Trigger a `develop` push release and confirm the created prerelease tag's commit SHA equals the pushed develop commit SHA (not the `main` branch HEAD), by inspecting the GitHub Release page / `git ls-remote --tags`. This verifies the "Scenario: develop tag lands on develop commit not main".

## 2. AI changelog generation

- [x] 2.1 In the `release` job, replace the existing `Generate changelog` step's grep logic with a primary path that resolves the previous release tag (`git describe --tags --abbrev=0`), computes `git diff` of byok-relevant paths (`cmd/ internal/ openspec/specs/`) between that tag and `HEAD` (all changed files for the first release), and assembles the diff into a prompt context string. This implements the "Decision: Generate the changelog via GitHub Models using a release-ranged diff prompt" and the "Decision: Scope the diff sent to the model to byok-relevant paths". Verification: workflow run logs show a non-empty diff context assembled from those paths only.
- [x] 2.2 Add a GitHub Models model call (HTTP request to the GitHub Models inference endpoint, authenticated with `GITHUB_TOKEN` or a dedicated Models token secret) that sends the assembled prompt and writes the model's Markdown output — categorized into 新增功能 / 優化功能 / 修復功能 — to `changelog.md`. This satisfies the "AI-generated categorized changelog for GitHub Releases" requirement. Verification: a successful release run produces a `changelog.md` whose body contains the three categorization section headings.
- [x] 2.3 Add a fallback that, when the GitHub Models call exits non-zero, returns an empty body, or the Models token is unset, regenerates `changelog.md` from `git log --pretty=format:"%s"` between the previous tag and `HEAD` using the conventional-commit prefix categorization (新增功能 / 優化功能 / 修復功能). This retires the legacy "Categorized changelog generated from commit history for GitHub Releases" requirement as the primary path while keeping it as the fallback, and implements the "Decision: Retain a commit-history fallback step" and the "Scenario: Model call failure falls back to commit-history changelog". Verification: a release run with an unset/invalid Models token still completes and the release body contains a commit-history categorized changelog.
- [x] 2.4 Confirm the `softprops/action-gh-release` step still consumes `body_path: changelog.md` so the AI (or fallback) changelog becomes the release body for both stable and prerelease workflows. Verification: the release action step in the workflow YAML references `body_path: changelog.md`.
