## 1. internal/updater 套件核心

- [x] 1.1 實作 channel 判定：在 `internal/updater` 新增 `Channel(v string) string`，版本含 `-dev.` 回傳 `"dev"`、否則 `"stable"`（對應 Decision: Channel 判定以版本字串含 `-dev.` 為準）。驗證：`internal/updater/updater_test.go` 的 `TestChannel` 覆蓋 `0.1.0`→stable、`0.1.1-dev.42`→dev、`dev`→stable 三情境。
- [x] 1.2 實作 Channel-aware version comparison：新增 `IsNewer(current, latest string) (bool, error)`，dev channel 以 `-dev.<N>` 數字比較、stable 以 semver 比較、當前為字面 `dev` 一律回傳 false（滿足 Requirement: Channel-aware version comparison）。驗證：`TestIsNewer_DevByRunNumber`（42<50 true）、`TestIsNewer_StableSemver`（0.1.0<0.1.1 true）、`TestIsNewer_LocalDevNotNewer`（`dev` vs 任意 false）、`TestIsNewer_DevParseError`（畸形 `-dev.x` 回傳 error）。
- [x] 1.3 實作 GitHub Releases 查詢（對應 Interface / data shape）：新增 `LatestRelease(ctx, channel) (Release, error)` 呼叫 `GET https://api.github.com/repos/IISI-2209026/LlmByok/releases` 帶 `Accept: application/vnd.github+json`，依 channel 過濾 `prerelease` 旗標並取最新（dev 取 `-dev.<N>` 最大、stable 取 semver 最大）；定義 `Release{Tag,Version,Prerelease,Assets}` 與 `Asset{Name,DownloadURL}`（對應 Decision: 以 GitHub Releases API 列舉並選最新 release）。驗證：`TestLatestRelease_DevChannel` 與 `TestLatestRelease_StableChannel` 以 `httptest` stub 回傳固定 JSON，斷言選中正確 release；`TestLatestRelease_NetworkError` 模擬 5xx 回傳 error。
- [x] 1.4 實作資產選擇：新增 `SelectAsset(rel Release, goos, goarch string) (Asset, bool)`，以 `byok-<version>-<goos>-<goarch>.<ext>`（windows→`zip`、linux/darwin→`tar.gz`）完全比對 `rel.Assets` 名稱（對應 Decision: 資產選擇以 GOOS/GOARCH 對應命名比對）。驗證：`TestSelectAsset` 表格化測試 windows/amd64、linux/amd64、darwin/arm64、找不到資產回傳 false。
- [x] 1.5 實作下載與解壓：新增 `downloadAndExtract(ctx, asset, goos) ([]byte, error)`，以 `net/http` 下載資產，windows 以 `archive/zip`、linux/darwin 以 `compress/gzip`+`archive/tar` 解出檔名為 `byok`（或 `byok.exe`）的成員位元組（對應 Decision: 不引入第三方 Go 相依）。驗證：`TestDownloadAndExtract_Zip` 與 `TestDownloadAndExtract_TarGz` 以 stub HTTP server 回傳預先組好的 zip/tar.gz，斷言取出的位元組與放入的 `byok` 內容一致。
- [x] 1.6 實作原子自我替換：新增 `DownloadAndReplace(ctx, rel, goos, goarch) error`，以 `os.Executable()` 取當前路徑、寫入同目錄暫存檔、`os.Chmod 0755`，Unix 以 `os.Rename` 覆蓋、Windows 以 `MoveFileEx`+`MOVEFILE_REPLACE_EXISTING` 覆蓋，失敗清理暫存檔（對應 Decision: 原子自我替換採平台對應策略）。驗證：`TestDownloadAndReplace_ReplaceTarget` 以介面注入可替換的目標路徑與 stub release，斷言目標檔內容被替換為新位元組且可執行；`TestDownloadAndReplace_AssetNotFound` 與 `TestDownloadAndReplace_NetworkError` 斷言原檔不變。

## 2. `byok update` 子指令

- [ ] 2.1 新增 `cmd/update.go` 實作 `byok update` 子指令（對應 Implementation Contract: Behavior 與 Failure modes）：解析當前版本→判定 channel（或以 `--channel prerelease|release` 覆寫）→`LatestRelease`→`IsNewer`，已有最新印「已是最新版本 (<version>)」exit 0；有新版印「更新中：<current> → <latest>」→ `DownloadAndReplace`→印「已更新至 <latest>」exit 0；`--check` 只印最新/目前不替換；`--channel` 不合法值（非 `prerelease`/`release`）印錯誤 exit 1 不發請求；找不到資產印錯誤 exit 1；網路失敗印錯誤 exit 1（滿足 Requirement: `byok update` self-update command；對應 Decision: `byok update` 提供 `--channel` 旗標覆寫 channel 判定；對應 Decision: `byok update` 提供 `--check` 旗標只檢查不替換）。驗證：`cmd/update_test.go` 的 `TestUpdateCmd_NoUpdate`、`TestUpdateCmd_CheckOnly`（以 stub updater 注入，斷言目標檔未被修改）、`TestUpdateCmd_ChannelOverride_Prerelease`（stable 版 + `--channel prerelease`，斷言查 dev release）、`TestUpdateCmd_ChannelOverride_Release`（dev 版 + `--channel release`，斷言查 stable release）、`TestUpdateCmd_InvalidChannel`（`--channel beta` exit 1 不發請求）、`TestUpdateCmd_AssetNotFound`（exit 1）、`TestUpdateCmd_NetworkError`（exit 1）、`TestUpdateCmd_UpdatesBinary`（stub 注入新 release，斷言可替換的目標檔被替換）。
- [ ] 2.2 在 `cmd/root.go` 註冊 `byok update` 子指令，使 `byok --help` 列出 update 且 `byok update --help` 顯示 `--check` 與 `--channel` 旗標及說明。驗證：`go build ./...` 通過；`go run main.go update --help` 同時顯示 `--check` 與 `--channel`。

## 3. 啟動版本檢查

- [ ] 3.1 在 `cmd/root.go` 根指令設定 `PostRunE`：於子指令完成後呼叫 updater 查詢當前 channel 最新 release，較新時在 stderr 印一行含最新版、目前版與 `byok update` 文字；以 `cmd.Name()` 為 `"launch"` 或 `"update"` 時跳過；`BYOK_NO_UPDATE_CHECK=1` 跳過；HTTP timeout 3 秒，任何錯誤靜默不改 exit code/stdout（滿足 Requirement: Startup version check after non-launch commands，對應 Decision: 啟動版本檢查置於 root 指令 PostRunE，對 launch 與 update 跳過）。驗證：`cmd/root_postrun_test.go` 的 `TestRootPostRun_PrintsHintOnNewer`（注入 stub updater 回傳新版本，stderr 含 `byok update`）、`TestRootPostRun_NoHintWhenUpToDate`、`TestRootPostRun_SkipsLaunch`（執行 launch 後無查詢）、`TestRootPostRun_SkipsUpdate`、`TestRootPostRun_NetworkSilent`（stub 回 error，stderr 無提示、exit code 不變）、`TestRootPostRun_EnvDisabled`（`BYOK_NO_UPDATE_CHECK=1` 不發請求）。

## 4. README 安裝與 update 說明

- [ ] [P] 4.1 在 `README.md` 新增「安裝」段落，並列兩種方式：`go install github.com/IISI-2209026/LlmByok@latest` 與自 GitHub Releases 下載 `byok-<version>-<os>-<arch>.<ext>` 預建二進位（解壓、放 PATH、`byok --version` 驗證），並註明 Releases 安裝為啟用 `byok update` 自我更新的建議路徑（滿足 Requirement: README documents prebuilt binary installation via GitHub Releases）。驗證：內容審視確認兩種安裝方式皆有步驟、資產命名格式正確、提及 `byok update`。
- [ ] [P] 4.2 在 `README.md` 使用說明區加入 `byok update` 條目：說明依當前 channel（dev/stable）查 GitHub Releases、下載對應平台資產並替換執行檔；說明 `--check` 只查詢不替換；說明 `--channel prerelease|release` 覆寫自動 channel 判定以更新到預發布或正式版本；附至少一個範例（含 `--channel` 與 `--check`）（滿足 Requirement: README documents prebuilt binary installation via GitHub Releases 的 Update command documented scenario）。驗證：內容審視確認 `byok update`、`--check`、`--channel` 皆有說明與範例。

## 5. 整體驗證

- [ ] 5.1 執行 `go vet ./...` 與 `go test ./...`（對應 Acceptance criteria；Windows 本機不啟用 `-race`，委由 CI）全部通過；手動 `go run main.go update --check` 對真實 repo 顯示最新 stable 版號、`go run main.go update --channel prerelease --check` 顯示最新 dev 版號。驗證：`go vet` 無輸出、`go test` 全綠、兩個 `update --check` 指令印出版號。
- [ ] 5.2 更新 `AGENTS.md`「套件職責」表格新增 `internal/updater` 列，並在「CLI 介面」/「開發規範」補 `byok update`（含 `--channel`/`--check`）與啟動檢查行為，以及在 Release workflow 相關段落補 changelog 產生說明（依專案維護規則；對應 Scope boundaries 的 in scope 項目）。驗證：內容審視確認表格新增列、`byok update` 行為與 changelog 產生已記載。

## 6. Release changelog 產生

- [ ] 6.1 修改 `.github/workflows/release.yml` 的 `release` job：在 `softprops/action-gh-release` 步驟前新增「Generate changelog」shell step，以 `git describe --tags --abbrev=0` 取上一個 release tag（無 tag 時以全部 commit 為範圍），`git log --pretty=format:"%s" <range>` 取 commit subject，依 conventional commit prefix 分類輸出 Markdown 至 `changelog.md`：`feat:` → `## 新增功能`、`refactor:`/`perf:` → `## 優化功能`、`fix:` → `## 修復功能`，其餘 prefix 不列入這三段（對應 Decision: Release changelog 以 conventional commit 分類產生，滿足 Requirement: Categorized changelog generated from commit history for GitHub Releases）。驗證：內容審視確認 step 存在、三個分類標題與 prefix 對應正確、無 tag 分支處理。
- [ ] 6.2 修改 `softprops/action-gh-release` 步驟：移除 `generate_release_notes: true`，改以 `body_path: changelog.md`（或等價 `body: ${{ steps.changelog.outputs.body }}`）將分類 changelog 作為 release body（滿足 Requirement: Categorized changelog generated from commit history for GitHub Releases）。驗證：內容審視確認 `generate_release_notes` 已移除且 body 來自 `changelog.md`；YAML lint（`actionlint` 或 `yamllint` 若專案已用，否則 `python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`）通過。
